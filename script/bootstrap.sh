# add github cli extension for gei
gh extension install github/gh-gei


#install brew if this is not in the path
# if [which -s brew]; then
#     echo "brew already installed"
# else
#     echo "installing brew"
#     /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
# fi

#add powershell to mac
brew install --cask powershell

#copy .env file variables to shell - this doesn't work in this shell script
# echo "copying .env file variables to shell"
# set -o allexport; source .env; set +o allexport

